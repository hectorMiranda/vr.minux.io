# UI Tabs

**Category:** UI &middot; **XR:** vr &middot; **Tags:** `ui`, `tabs`, `panel`

A tabbed panel with three tabs that cycle through different content views.

## Run it

Open the launcher and pick **UI Tabs**, or deep-link straight to it:

```
http://localhost:5173/#ui-tabs
```

## What this verifies

Verifies spatial UI widgets (panels, buttons, sliders, toggles) render and respond to pointer/controller input.

## Source

[`src/scenes/ui-tabs.js`](../../src/scenes/ui-tabs.js)
