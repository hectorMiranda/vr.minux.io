# UI Buttons

**Category:** UI &middot; **XR:** vr &middot; **Tags:** `ui`, `button`, `interaction`

Three Button3D widgets that change a cube\

## Run it

Open the launcher and pick **UI Buttons**, or deep-link straight to it:

```
http://localhost:5173/#ui-buttons
```

## What this verifies

Verifies spatial UI widgets (panels, buttons, sliders, toggles) render and respond to pointer/controller input.

## Source

[`src/scenes/ui-buttons.js`](../../src/scenes/ui-buttons.js)
